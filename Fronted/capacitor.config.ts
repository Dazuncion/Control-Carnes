import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.dazuncion.controlcarnes',
  appName: 'ControlCarnes',
  webDir: 'build'
};

export default config;
